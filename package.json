{
  "name": "http-error-stream",
  "version": "1.0.0",
  "description": "Stream an error over http",
  "main": "index.js",
  "scripts": {
    "deps": "dependency-check . && dependency-check . --extra --no-dev",
    "test": "standard && npm run deps && NODE_ENV=test node test",
    "test:cov": "standard && npm run deps && NODE_ENV=test istanbul cover test.js"
  },
  "repository": "yoshuawuyts/http-error-stream",
  "keywords": [
    "http",
    "error",
    "stream"
  ],
  "license": "MIT",
  "dependencies": {
    "from2-string": "^1.1.0",
    "readable-stream": "^2.0.6"
  },
  "devDependencies": {
    "dependency-check": "^2.5.1",
    "istanbul": "^0.4.2",
    "tape": "^4.5.1"
  }
}
